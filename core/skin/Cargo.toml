[package]
name = "core-skin"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "跨端皮肤 JSON v1/v2 的纯数据模型与解析（无平台依赖）"

[lib]
name = "core_skin"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
csscolorparser = "0.8"
