[package]
name = "clipboard-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "剪贴板历史存储：SQLite 持久化、去重、搜索与留存策略（平台无关）"

[dependencies]
rusqlite = { version = "0.40", features = ["bundled"] }
sha2 = "0.11"

[dev-dependencies]
tempfile = "3"
