org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# 版本唯一事实源是仓库根 version.json，由 scripts/set-version.ps1 统一 bump；
# 本文件不得再固定 SHURUFA_VERSION_*，否则版本会从 version.json 漂移（曾 0.4.0/9 vs 0.4.1/10）。
