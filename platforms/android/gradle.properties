org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# 版本单一事实源是仓库根 version.json，由 app/build.gradle.kts 构建期直接读取。
# 此处禁止固定 SHURUFA_VERSION_*：会与 version.json 漂移（曾 0.4.0/9 vs 0.4.1/10），
# set-version.ps1 -Check 会主动拦截。本地试包可用 -PSHURUFA_VERSION_CODE=n 临时覆盖。
