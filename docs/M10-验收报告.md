# M10 验收报告（专业与无障碍 + 困难项攻克 → v1.1.0）

> 验收日期：2026-08-18 ｜ 目标版本：v1.1.0（versionCode 33）｜ 计划依据：[开发计划](开发计划.md) M10
> 对应搜狗时间线：16.2 专业词模式 / 12.0 无障碍 / 8.2a 语音 / 14.4 GB18030 / 7.5 生僻字 / 5.2 节气提醒

## 一、困难项攻克（M8/M9 遗留）

| 困难项 | 方案 | 验证 |
|---|---|---|
| 文件发送失败重试 | host msg_id→原路径台账（PENDING_FILE_SENDS）+ SendFile 重试载荷，重发走广播通道 | host 单测（台账登记/取回/清理） |
| 交互式配对 UI | 设置中心配对向导：host `pair-ui` 发起端 + prompt/confirm/result 文件交互（token 防串扰） | settings 状态机单测（结果/确认码/超时/空闲）+ 冒烟 |
| 「？？？」表情 | 实测 librime 无法累积标点组合 → TSF 按键级替代（Shift+/ 前两个全角「？」，第三个 🤔） | TSF 状态机单测 + 引擎基线测试 |
| 简拼开关 | librime 无条件规则 → 部署期替代：gen-nojianpin-schema.ps1 生成无 abbrev 变体方案，schema_list 部署后可加载、全拼不回归 | 引擎集成测试 |

## 二、M10 功能交付

| 项 | 功能 | 落地方式 | 验证 |
|---|---|---|---|
| M10-1 | 专业词模式 | 三套场景词库（医生/律师/代码）部署即生效 + 输入页场景下拉 + options.scenario_dict | options/settings 单测 + 引擎端到端（场景词可打、常规拼音不回归） |
| M10-2/3/4 | 无障碍/语音/GB18030 评估 | [docs/M10-评估报告.md](docs/M10-评估报告.md)：读屏 v1.2 UIA、语音云端试点、GB18030 不建议全量 | 评估文档 |
| M10-5 | 生僻字字库扩充 | U+ 输入兜底（已实装）覆盖任意码位 + rime-ice 大字库 | 评估结论 |
| M10-6 | 节日/节气提醒 | jieqi/jieri 候选（C 世纪常数近似 ±1 天；lua quality=100 置顶） | 引擎集成测试 |

## 三、调试基础设施（Tauri MCP）

- 设置中心接入 `tauri-plugin-mcp-bridge`（feature `mcp-bridge` 默认关，127.0.0.1:9223）；
- DSH 侧 cordis.patch.yml 注册 `mcp-tauri` 实例（dsh-mcp-client → @hypothesi/tauri-mcp-server），新会话工具以 `mcp__tauri__*` 出现；
- 端到端实测：initialize → 20 工具 → driver_session 连接 → manage_window 读到 FOX输入法主窗口。

## 四、验证证据

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --locked` | ✅ **206 passed / 0 failed**（较 M9 的 199 增加 7 项：M10 新测试） |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 退出码 0 |
| `cargo fmt --check` | ✅ 0 diff |
| `npm run build`（设置中心） | ✅ 通过（专业词下拉已打包） |
| `set-version.ps1 -Check` | ✅ 1.1.0 / 33 一致 |

## 五、实测结论与暂缓项（均已记录）

| 项 | 结论 |
|---|---|
| librime 列表 patch 追加 | ⛔ 不支持 `"+item"`（会把 translators 整体替换、拼音失效）→ 场景词库以"文件部署即生效"交付 |
| 简拼开关效果 | 依赖 prism 部署增量，集成环境无法断言（rime_ice 默认简拼码亦无候选），留实机 |
| 读屏 / 语音 | 分阶段：v1.1 上屏可达；v1.2 UIA Provider / 云端转写试点 |

## 六、验收结论

M10 六项全部落地，四项遗留困难项全部攻克并有实证；版本由 1.0.0（code 32）bump 至
**1.1.0（code 33）**，CHANGELOG 已落版。
