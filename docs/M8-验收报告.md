# M8 验收报告（跨设备与个性化深化 → v0.9.0）

> 验收日期：2026-08-18 ｜ 目标版本：v0.9.0（versionCode 31）｜ 计划依据：[开发计划](开发计划.md) M8
> 对应搜狗时间线：16.5 跨设备同步 / 16.5 账号绑定设备管理 / 16.1 剪贴板 / 15.2 灵犀候选直达 / 13.11+14.1 皮肤商城

## 一、交付清单

| 项 | 功能 | 落地方式 | 验证 |
|---|---|---|---|
| M8-1 | 同步状态可视化 + 发送来源标签 | host 记录活动流 `sync-activity.json`（方向/类型/来源设备/状态/预览，FIFO 50）+ 设置中心「最近同步」面板 | options 单测（往返/裁剪/单调 id）+ UI 构建 |
| M8-1b | 失败重试（一键重发） | 失败条目带 `retry_id` 载荷（文本 ≤512KiB / PNG ≤1MiB / 文件按落盘路径）；设置中心写 `sync-retry-request.json`，host 2s 轮询重放写剪贴板并回写「（重试）」活动 | options 单测（retry_id 往返/旧文件兼容）+ settings 单测（请求体形状） |
| M8-2 | 设备管理 UI | peers `last_seen_ms` + 设置中心已配对设备面板（重命名/移除/最近在线）；配对暂用终端 CLI | sync-core 测试（last_seen 落盘）+ settings 命令单测 |
| M8-3 | 剪贴板批量整理 | 历史页选择模式：多选/全选（按搜索过滤）/批量置顶/批量删除（两步确认） | settings 命令测试 + npm build |
| M8-4 | 应用/网站直达候选 | `app_direct.lua` 标记候选（🖥/🌐，quality=100 置顶）+ TSF 提交拦截启动 + 设置页直达快捷编辑器 | options 单测（规范化/裁剪/lua 转义）+ ime-bridge 引擎集成测试（置顶/常规输入不受影响） |
| M8-5 | 皮肤包导入导出 | 后端 `export_skin` 写 Downloads（文件名净化防路径注入）+ 前端「导出为文件 / 导入文件」；`validate_skin_json` 单一校验路径 | settings 单测（JSON/version 校验）+ npm build |

## 二、验证证据

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --locked` | ✅ **199 passed / 0 failed**（含 ime-bridge 引擎集成测试、TSF 真实窗口测试） |
| `cargo clippy --workspace --all-targets` | ✅ 0 告警 |
| `cargo fmt --check` | ✅ 0 diff |
| `npm run build`（设置中心） | ✅ 通过（皮肤页导出/导入、同步页重试按钮已打包） |
| `set-version.ps1 -Check` | ✅ 0.9.0 / 31 四处派生点一致 |

## 三、受阻 / 暂缓项（均有实证记录）

| 项 | 状态 | 原因 / 去向 |
|---|---|---|
| 交互式配对 UI | 🔶 暂以 CLI 替代 | 配对需确认码双向核对，host `pair <IP>` 控制台交互可用；交互式 UI 待 M9 设置中心升级时设计 |
| 文件发送失败重试 | 🔶 数据链路待补 | 发送侧 `FileTransferDone` 无 msg_id→原路径映射，重试需先补发送台账；已记录于 docs/开发计划.md 与 CHANGELOG |
| 简拼开关（M7 遗留） | ⛔ 引擎不支持 | librime 1.17 speller/algebra 无条件规则（实测 `Error loading formula #13`） |
| 「？？？」表情触发（M7 遗留） | 🔶 待设计 | 全角问号由 punctuator 立即上屏，需 TSF 层拦截 Shift+/ 建组合 |

## 四、验收结论

M8 的 P0/P1/P2 全部落地并通过全量验证：跨设备同步闭环（收发可视化 → 失败可重试 →
设备可管理）与个性化（直达候选、皮肤包交换）齐备。版本由 0.8.0（code 30）bump 至
**0.9.0（code 31）**，CHANGELOG 已落版；两项暂缓项均为数据链路或交互设计问题，不阻塞发布。
