# M11 / v1.2 验收报告（读屏无障碍 + 语音云端转写试点 + 生僻字词库包）

> 验收日期：2026-08-19 ｜ 目标版本：v1.2.0（versionCode 34）｜ 计划依据：[开发计划](开发计划.md) M10+
> 对应搜狗时间线：12.0 无障碍 / 8.2a 语音 / 14.4 GB18030 / 7.5 生僻字

## 一、交付清单

| 项 | 功能 | 落地方式 | 验证 |
|---|---|---|---|
| v1.2-1 | 语音输入云端转写试点 | waveIn 16kHz 录音（audio_capture.rs）→ OpenAI 兼容 /v1/audio/transcriptions（asr.rs）→ Partial/Final 复用书面语化+粘贴链路；设置中心语音页（后端下拉/Base URL/模型，Key 只读环境变量） | host 单测 +6（WAV/multipart/响应/配置）；实机麦克风与真实 API 留用户验证（无 Key 面板提示） |
| v1.2-2 | 读屏无障碍 | Windows 候选窗 UIA Provider（IRawElementProviderSimple：Name=候选文本/ControlType=Text，WM_GETOBJECT 注册）；Android 候选词 importantForAccessibility + contentDescription + TYPE_WINDOW_CONTENT_CHANGED | TSF 编译+51 测试；Android APK 重建；完整 ITextProvider 列阶段二 |
| v1.2-3 | 生僻字词库包 + GB18030 合规清单 | 449 字内联进 rime_ice.dict.yaml（词库驱动 437 + 扩展 B 12）；设置中心场景下拉「生僻字」；[GB18030-2022-合规清单](GB18030-2022-合规清单.md) | 引擎集成测试 weng→齆/nang→齉/nihao 不回归；运行时探针 362/449 可打 |

## 二、重要实测结论（修正）

- **librime import_tables 对补充词库不生效**：实测词条不进编译表（num_entries 不变），
  M10-1"场景词库部署即生效"实际靠 base 词库恰好收录场景词才成立；场景词条与生僻字
  词条统一内联进 rime_ice.dict.yaml 修正（见 58c5e64）。
- 语音 stub 换真实链路：面板标题随后端显示（云端转写 / dev-stub）。

## 三、验证证据

| 检查 | 结果 |
|---|---|
| cargo test --workspace --locked | ✅ 全绿（约 220 项，含引擎集成 + TSF 真实窗口） |
| cargo clippy --workspace --all-targets | ✅ 0 告警 |
| cargo fmt --check | ✅ 0 diff |
| npm run build（设置中心） | ✅ 通过 |
| set-version.ps1 -Check | ✅ 1.2.0 / 34 一致 |

## 四、阶段二（已记录，不阻塞 v1.2）

- Windows 候选窗完整 ITextProvider（逐候选范围朗读/导航）
- 语音本地 Whisper / 更多 ASR 服务商适配
- Android 无障碍随系统读屏联调实机验证（本机已装 v1.2.0 APK，USB 重连后可 adb 安装）

## 五、验收结论

v1.2 三项全部落地并有测试/文档证据；版本由 1.1.0（code 33）bump 至 **1.2.0（code 34）**。

