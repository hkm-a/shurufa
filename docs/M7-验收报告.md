# M7 验收报告（候选与打字体验收尾 → v0.8.0）

> **历史快照**：本报告记录该里程碑当时的验收情况，**不代表当前状态**。
> 当前质量信号以 CI 为准（见 README 徽章与 `scripts/` 下的四个门禁脚本）；
> 定位与理由见 [文档管理](文档管理.md) §9。

> 验收日期：2026-08-18 ｜ 目标版本：v0.8.0（versionCode 30）｜ 计划依据：[开发计划](开发计划.md) M7
> 对应搜狗时间线：16.3b 候选条重构 / 16.6 打字模型 / 16.1 状态栏 / 15.9 表情推荐 / 16.2 专业词（后置）

## 一、交付清单

| 项 | 功能 | 落地方式 | 验证 |
|---|---|---|---|
| M7-1 | 候选面板模式选项（single / multi） | options.json `candidate_panel_mode` + TSF 每键热读 + 指纹失效 | settings DTO 往返/缺省测试 |
| M7-2 | 设置页候选面板下拉 | 输入页新增下拉（数据通用保存通道） | npm build 通过 |
| M7-3 | 多行候选面板布局 | GDI/D2D/DComp 三后端共享 `Item.row`，5+4 两行，命中按行映射 | TSF 真实窗口集成测试（90px<130px、第二行命中） |
| M7-4 | ↓ / 翻页语义 | multi 模式即多行面板，翻页走引擎既有按键路径 | 引擎集成测试回归 |
| M7-6 | 候选条右键菜单 | 复制 / 删词 / 降频 / 隐藏 / 打开设置中心；`{Down}`×index + `{Control+d/j/x}` 会话共享钩子 | 3 单测 + 2 引擎集成（删词/降频，非空防假阳性） |
| M7-7 | 悬浮球不透明度 | 通用页滑杆（30–100%）+ Tauri setOpacity 即时生效 | DTO 序列化/缺省/钳位测试 |
| M7-8 | 候选准确率优化（上下文调频） | translator `contextual_suggestions: true`（语境词频加权） | 引擎集成测试（不劣化排名） |
| M7-9 | 表情推荐时机 | `emoji_timing.lua`：okok→👌、aini→❤️、wanan→🌙 | 引擎集成测试 4 断言 |
| 修复 | cold_word_drop 降频崩溃（既有 bug） | processor/filter 统一优先 reduce_freq_words + 空表文件补齐 | 引擎集成测试锁定降频链路 |

## 二、验证证据

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --locked` | ✅ **172 passed / 0 failed**（含 ime-bridge 17 项引擎集成测试、TSF 48 项） |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 0 告警 |
| `cargo fmt --check` | ✅ 0 diff |
| `npm run build`（设置中心） | ✅ 通过 |
| `set-version.ps1 -Check` | ✅ 0.8.0 / 30 四处派生点一致 |

## 三、受阻 / 暂缓项（均有实证记录，见 docs/优化灵感库.md）

| 项 | 状态 | 原因 |
|---|---|---|
| M7-5 候选 Tab 多服务切换 | 🔶 待方案评估 | librime 无原生多服务候选源（迭代方向附录 D） |
| 简拼开关 | ⛔ 引擎不支持 | librime 1.17 speller/algebra 无条件规则（实测 `Error loading formula #13`） |
| 「？？？」表情触发 | 🔶 待设计 | 全角问号由 punctuator 立即上屏，需 TSF 层拦截 Shift+/ 建组合 |

## 四、验收结论

M7 的 P0/P1 项全部落地并通过全量验证；受阻项为 librime 引擎能力边界，已记录且不阻塞 v0.8.0。
版本由 0.7.1（code 29）bump 至 **0.8.0（code 30）**，CHANGELOG 已落版。
