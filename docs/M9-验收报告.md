# M9 验收报告（设置中心与 AI 助手整合 → v1.0.0）

> **历史快照**：本报告记录该里程碑当时的验收情况，**不代表当前状态**。
> 当前质量信号以 CI 为准（见 README 徽章与 `scripts/` 下的四个门禁脚本）；
> 定位与理由见 [文档管理](文档管理.md) §9。

> 验收日期：2026-08-18 ｜ 目标版本：v1.0.0（versionCode 32）｜ 计划依据：[开发计划](开发计划.md) M9
> 对应搜狗时间线：16.7 设置中心重构 / 16.1c 光标唤起与窗口跟随 / 16.4 划词扩展 / 15.4d 快捷搜索 / 15.3+11.0 AI 工具箱

## 一、交付清单

| 项 | 功能 | 落地方式 | 验证 |
|---|---|---|---|
| M9-1 | 设置中心全新升级 | 分组导航侧栏（概览/输入/效率/外观/系统）+ 顶栏全页搜索（12 页静态索引下拉直达）+ 未保存提示（皮肤/直达/按应用/短语 4 类编辑区打脏，切页/收起确认） | npm build + 既有单测回归 |
| M9-2 | AI 工具整合入口 | 悬浮条主菜单新增「AI 助手」：AI 帮写（host `ai show` → WM_AI_EXTERNAL_SHOW 外部唤起）+ 划词热键引导 + AI 设置直达；后台服务启动预热面板窗口 | host build + 冒烟（ai show 用法/未创建提示） |
| M9-3 | 桌面快捷搜索 | 悬浮条子菜单内嵌搜索框：应用（Start Menu .lnk + App Paths，ShellExecute 启动）、文件（桌面/文档/下载 ≤3000 项遍历，资源管理器定位）、计算器（meval 求值，结果复制） | 纯函数单测：算式边界/.lnk 扫描/文件遍历 |
| M9-4 | 窗口跟随 | AI/划词面板可见期间 200ms 定时器轮询输入锚点重定位（保持宽高，光标不可用回退鼠标位置），隐藏停表 | host build + clippy |
| M9-5 | AI 光标助手 | Word/WPS 白名单（WINWORD/WPS/WPS Office/ET/WPP）识别：面板标题显示「AI 光标助手 · Word/WPS」，草稿粘贴到光标处 | host build + clippy |
| M9-6 | AI 划词场景扩展 | 划词应用白名单：GeneralSettings 新字段 + 通用页每行一 exe 编辑器（空=全部放行）；host 划词热键按前台 exe 判定，未命中跳过留日志 | options/settings 全链路单测（DTO 往返/规范化） |

## 二、验证证据

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --locked` | ✅ **199 passed / 0 failed**（含 ime-bridge 引擎集成、TSF 真实窗口测试） |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ 退出码 0（唯一提示为 meval 传递依赖 nom v1.2.4 的 future-incompat 说明，非本项目代码） |
| `cargo fmt --check` | ✅ 0 diff |
| `npm run build`（设置中心） | ✅ 通过（导航/搜索/白名单/AI 菜单/桌面搜索已打包） |
| `set-version.ps1 -Check` | ✅ 1.0.0 / 32 四处派生点一致 |

## 三、暂缓 / 说明项

| 项 | 状态 | 说明 |
|---|---|---|
| 划词润色/翻译 UI 点击唤起 | 🔶 依赖热键 | 前台选区抓取需热键上下文（GetGUIThreadInfo），悬浮条菜单以热键引导呈现，与既有行为一致 |
| 文件搜索范围 | 🔶 桌面/文档/下载 | 非全盘索引（未接入 Windows Search COM）；已在 CHANGELOG 注明，后续可升级为 Everything 式索引 |
| 简拼开关 / 「？？？」表情（M7 遗留） | ⛔ 引擎边界 | librime 1.17 无条件规则；需 TSF 拦截，见 docs/优化灵感库.md |

## 四、验收结论

M9 的 P0/P1/P2 全部落地并通过全量验证：设置中心从「列表页」升级为「分组导航 +
全页搜索 + 未保存防护」，AI 能力（帮写/润色/翻译）在悬浮条有了统一入口与外部唤起
通道，桌面快捷搜索（应用/文件/计算器）与窗口跟随、光标助手、划词白名单齐备。
版本由 0.9.0（code 31）bump 至 **1.0.0（code 32）**，CHANGELOG 已落版。
